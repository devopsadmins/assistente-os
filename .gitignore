node_modules/
dist/
build/
*.tsbuildinfo
.env
.env.local
config.local.json
*.db
*.db-journal
*.db-wal
*.db-shm
.DS_Store
logs/

# Artefatos gerados (regeneráveis) — permanecem só no disco local
docs/graphify-out/
docs/archify/
docs/assets/
# bundle concatenando todos os docs/*.md num arquivo só
docs/KNOWLEDGE-BASE.md

# Claude Code local settings
.claude/

# Transcrições de sessão de agente e credenciais locais — nunca versionar
# (podem conter segredos, PII e prompts). Writeups deliberados vão em docs/.
session-*.md
archive/sessions/session-ses_*.md
auth.json

# Screenshots de trabalho (browser MCP, capturas da UI)
.playwright-mcp/
.web-shots/
/*.png

# Saída gerada a cada run de teste (Playwright) — nunca versionar
test-results/
playwright-report/

# Artefato local do `vercel` CLI (vercel link/deploy) — amarrado à conta/projeto
# de quem rodou, não é portável entre máquinas.
.vercel/

# Material de trabalho de cliente — perfis, workshops, docs de engajamento.
# Nomes reais de clientes não entram no repo do produto; o lugar disso é a
# soul `consultoria_ia` (ver docs/ROADMAP.md e a nota de arquivamento).
tmp-clientes/
tmp-clientes-*/
