node_modules/
dist/
*.tsbuildinfo
.env
.env.local
config.local.json
*.db
*.db-journal
*.db-wal
*.db-shm
.DS_Store
logs/

# Artefatos gerados (regeneráveis) — permanecem só no disco local
docs/graphify-out/
docs/archify/
docs/assets/

# Claude Code local settings
.claude/

# Transcrições de sessão de agente e credenciais locais — nunca versionar
# (podem conter segredos, PII e prompts). Writeups deliberados vão em docs/.
session-*.md
archive/sessions/session-ses_*.md
auth.json

# Screenshots de trabalho (browser MCP, capturas da UI)
.playwright-mcp/
.web-shots/
/*.png

# Saída gerada a cada run de teste (Playwright) — nunca versionar
test-results/
playwright-report/
packages/ui/build/
