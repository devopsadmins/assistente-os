// Spike: nenhuma API exposta ao renderer ainda — a janela é só uma casca
// nativa sobre o SPA existente (packages/web), sem IPC. Se o spike validar
// a direção, APIs do tipo window.assistenteOS.* entram aqui depois via
// contextBridge.
