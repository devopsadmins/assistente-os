---
interface Props {
  class?: string;
  variant?: 'default' | 'success' | 'warning' | 'danger' | 'info';
  size?: 'sm' | 'md' | 'lg';
  dot?: boolean;
  children?: any;
}
const { class: className = '', variant = 'default', size = 'md', dot = false, children, ...attrs } = Astro.props;

const variantClasses = {
  default: 'bg-gray-100 text-gray-800',
  success: 'bg-green-100 text-green-800',
  warning: 'bg-yellow-100 text-yellow-800',
  danger: 'bg-red-100 text-red-800',
  info: 'bg-blue-100 text-blue-800',
};

const sizeClasses = {
  sm: 'px-2 py-0.5 text-xs',
  md: 'px-2.5 py-1 text-sm',
  lg: 'px-3 py-1.5 text-base',
};

const classes = `inline-flex items-center font-medium rounded-full ${variantClasses[variant]} ${sizeClasses[size]} ${className}`;
---

<span class={classes} {...attrs}>
  {dot && <span class="w-1.5 h-1.5 rounded-full bg-current mr-1.5" aria-hidden="true" />}
  <slot>{children}</slot>
</span>